const validateBody = require("./validateBody");

const validateById = require("./validateById");

const authenticate = require("./authenticate");

module.exports = { validateBody, validateById, authenticate };
